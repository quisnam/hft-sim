[package]
name = "client"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1", features = ["full"]}
rand = "0.8"
orderbook = { path = "../simple-orderbook/" }
server = { path= "../server/" }
